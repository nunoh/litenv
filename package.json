{
  "name": "litenv",
  "version": "0.1.0",
  "description": "Lightweight .env management, locally and over SSH",
  "author": "Nuno",
  "type": "module",
  "bin": {
    "litenv": "dist/cli.js"
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cloc": "cloc . --exclude-dir=node_modules,dist,.git --exclude-lang=JSON,Markdown --not-match-f='package-lock\\.json'",
    "test": "npm run build && node --test test/*.test.mjs",
    "test:coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-exclude='**/litenv-*/**' --test-coverage-exclude='test/**' --test-coverage-lines=92 --test-coverage-functions=90 test/*.test.mjs",
    "prepack": "npm run build",
    "prepublishOnly": "npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "keywords": [
    "dotenv",
    "env",
    "ssh",
    "cli",
    "configuration",
    "developer-tools"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nunoh/litenv.git"
  },
  "homepage": "https://github.com/nunoh/litenv#readme",
  "bugs": {
    "url": "https://github.com/nunoh/litenv/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT"
}
